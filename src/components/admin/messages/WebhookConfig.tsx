
import React from 'react';

// Componente vazio que não renderiza nada, já que estamos removendo essa funcionalidade
const WebhookConfig: React.FC<any> = () => {
  return null;
};

export default WebhookConfig;
